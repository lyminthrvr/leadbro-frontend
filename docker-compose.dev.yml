version: '3.8'
services:
  client:
    build:
      context: .
      dockerfile: Dockerfile.dev
    container_name: leadbro_frontend
    ports:
      - '3003:3000'
    volumes:
      - .:/usr/src/leadbro_frontend
      - /usr/src/leadbro_frontend/node_modules
    stdin_open: true
    tty: true